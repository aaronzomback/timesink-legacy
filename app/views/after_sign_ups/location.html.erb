

<h3>Where are you located?</h3>

<%= simple_form_for(@user, :url => wizard_path, :method => :put) do |f| %>
  <%= f.input :location, :placeholder => "i.e. Houston, TX", label: false %>
  <%= f.submit 'Next', :class => 'btn btn-primary' %>
<% end %>

<%= link_to 'skip', next_wizard_path %>
