

<div class="back-to-films">
  <%= link_to film_path(@film) do %>
  <span><%= image_tag "BackArrow1.svg" %> </span> <h3>Back to <%= @film.title %> </h3>
  <% end %>
</div>

<section class="review-content">
  <div class="">
    <h5><%= @review.title %></h5> <span><%= @review.created_at.strftime("%b %e, %Y")%></span>
    <p> Review by <u><%= link_to @review.user.username, user_path(@review.user) %></u></p>
  </div>
  <div class="review-body-content">
      <p class="review-body"><%= simple_format @review.body %></p>
  </div>
</section>


<% if is_logged_in? %>

<%= simple_form_for [@film, @review, Comment.new] do |f| %>
<%= f.text_area :body, placeholder: "Add a comment" %><br/>
<%= f.submit "Add Comment" %>
<% end %>

<% else %>

<h4> <%= link_to "Sign in to comment", new_session_path %></h4>

<% end %>

<h3>Comments</h3>




<ul>
  <%= render(partial: 'comments/comment', collection: @review.comments) %>
</ul>
